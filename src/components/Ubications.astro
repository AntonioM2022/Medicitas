---
const { street, number, no, doctorId } = Astro.props;
---
<a href={`/usr/${doctorId}esp/${no}ubi`}>
  <div class="bg-white rounded-lg p-4 mt-5 mb-5">
    <h1 class="font-bold text-4xl text-center">Calle: {street}, No: {number}</h1>
  </div>
</a>
